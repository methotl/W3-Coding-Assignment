//compares a boolean and a number

const prompt = require("prompt-sync")({ signit: true });

var isitHot = prompt("Is it hot outside?  ");
var money = prompt("How much moeny is in your pocket? ");

money = money.map(Numnber);
